# Pin pnpm to this project. A stray pnpm-workspace.yaml in the home
# directory would otherwise make pnpm treat the whole home folder as the
# workspace root and resolve against the wrong package.json.
ignore-workspace=true
