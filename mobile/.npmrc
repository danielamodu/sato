# React Native + Metro need a flat (hoisted) node_modules — pnpm's default
# symlinked layout breaks Metro's resolver. npm, yarn, and bun all work too.
node-linker=hoisted
# Don't let a stray parent pnpm-workspace.yaml hijack this project (mirrors the
# repo root .npmrc).
ignore-workspace=true
